@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Chuyển hướng về trang Login (true = force load để refresh lại trạng thái HTTP)
        Navigation.NavigateTo("/login", true);
    }
}